bool running = false;

int temperature = 30;

if (temperature > 30)
{
    running = true;
}

if (running == true)
{
    Console.WriteLine("motor is running");
}
else
{
    Console.WriteLine("motor is not running");
}